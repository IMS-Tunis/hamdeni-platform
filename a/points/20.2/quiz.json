{
  "questions": [
    {
      "type": "match",
      "question": "Match the file open mode to its description.",
      "pairs": {
        "'r'": "Open existing file to read",
        "'w'": "Create new or overwrite existing file for writing",
        "'a'": "Open file to add data at the end"
      }
    },
    {
      "type": "match",
      "question": "Match each reading function to its behavior.",
      "pairs": {
        "read()": "Whole file as one string",
        "readline()": "One line",
        "readlines()": "All lines as a list"
      }
    },
    {
      "type": "mcq",
      "question": "In the python code 'for line in f:', what naturally stops the loop?",
      "options": [
        "Manual counter",
        "Automatic end-of-file detection",
        "Timeout"
      ],
      "answer": "Automatic end-of-file detection"
    },
    {
      "type": "fill_blank",
      "question": "To remove trailing newline characters after reading a line, use ________.",
      "answer": "strip()"
    },
    {
      "type": "mcq",
      "question": "To start a new record on a new line when writing, end the string with:",
      "options": [
        "\\t",
        "\\n",
        "\\r",
        "a space"
      ],
      "answer": "\\n"
    },
    {
      "type": "mcq",
      "question": "Which construct closes the file automatically even if an error occurs?",
      "options": [
        "try/except",
        "if/else",
        "with open(...) as ...",
        "close()"
      ],
      "answer": "with open(...) as ..."
    },
    {
      "type": "fill_blank",
      "question": "The explicit method to release a file resource is ________.",
      "answer": "close()"
    },
    {
      "type": "match",
      "question": "Match the file organisation to its defining characteristic.",
      "pairs": {
        "Serial": "Arrival order, append at end",
        "Sequential": "Sorted by key",
        "Random": "Direct access via stored/computed position"
      }
    },
    {
      "type": "fill_blank",
      "question": "Random access relies on moving the ________ to a stored position before reading.",
      "answer": "file pointer"
    },
    {
      "type": "mcq",
      "question": "Which scenario warrants exception handling?",
      "options": [
        "Printing a constant string",
        "Converting user input to an integer that might be invalid",
        "Concatenating two fixed literals",
        "Assigning a variable to itself"
      ],
      "answer": "Converting user input to an integer that might be invalid"
    },
    {
      "type": "match",
      "question": "Match each block to its role.",
      "pairs": {
        "try": "Code that may raise an exception",
        "except": "Handle errors",
        "finally": "Always runs (cleanup)"
      }
    },
    {
      "type": "fill_blank",
      "question": "The specific exception for a missing file in the example is ________.",
      "answer": "FileNotFoundError"
    },
    {
      "type": "mcq",
      "question": "In the file-reading example, which parts may raise an exception?",
      "options": [
        "print(content) only",
        "with open(\"data.txt\",\"r\") and file.read()",
        "Only file.read()",
        "None"
      ],
      "answer": "with open(\"data.txt\",\"r\") and file.read()"
    },
    {
      "type": "match",
      "question": "Match each operation from the examples to the function used.",
      "pairs": {
        "Convert text mark to numeric for arithmetic": "int()",
        "Split a comma-separated record into fields": "split(\",\")"
      }
    }
  ]
}